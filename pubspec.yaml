name: vote_app
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 기본 패키지
  shared_preferences: ^2.1.1
  cupertino_icons:    ^1.0.8

  # FlutterFire 호환 버전 조합 (BoM v4.0.0 기준)
  firebase_core:   ^3.9.0      # ≥3.2.0 <4.0.0 :contentReference[oaicite:0]{index=0}
  firebase_auth:   ^5.3.3      # ≥3.2.0 <4.0.0 :contentReference[oaicite:1]{index=1}
  cloud_firestore: ^5.6.0      # ≥3.2.0 <4.0.0 :contentReference[oaicite:2]{index=2}

  # 인앱 구매: Flutter 3.24+ 대응 안정 버전
  in_app_purchase: ^3.2.3

# web 전용 firebase_auth 플러그인을 Core 3.x와 맞춰주는 override
dependency_overrides:
  firebase_auth_web: ^5.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
